// Return the first n prime numbers
// Sieve of Eratosthenes

#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>

void sieve(int n, uint32_t *primes, uint16_t *len)
{
  bool isPrime = calloc(n, sizeof(bool));
  uint32_t p =
}
